# GoodDeeds

A neighborhood task exchange web app enhancing local communities


## Installation

Rename `env_example` into `.env`

Create Google Cloud client for identification

As authorized javascript and redirect source use `http://localhost:8080`

Provide your keys into `.env` file

## Start

Run `docker compose up`
